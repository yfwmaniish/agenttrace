{
  "name": "@agenttrace/sdk",
  "version": "0.1.0",
  "description": "Agent instrumentation SDK — framework-agnostic with LangChain + Vercel AI adapters",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "clean": "rimraf dist",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@agenttrace/core": "workspace:*"
  },
  "license": "MIT"
}
